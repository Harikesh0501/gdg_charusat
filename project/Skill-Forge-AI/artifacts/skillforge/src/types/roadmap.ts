export interface RoadmapItem {
  id: number;
  title: string;
  detail: string;
  time: string;
  done: boolean;
}

export interface RoadmapPhaseMeta {
  index: string;
  title: string;
  period: string;
}
