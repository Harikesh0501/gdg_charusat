This directory's contents were not recoverable.

No dangling git blob matched `.agents/` — it was likely never `git add`-ed
before the accidental `git reset --hard` + `git clean -fd`. Nothing here was
fabricated. See the recovery report for full details.
